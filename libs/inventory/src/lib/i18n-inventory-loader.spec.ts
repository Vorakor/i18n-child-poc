import { I18nInventoryLoader } from './i18n-inventory-loader';

describe('I18nInventoryLoader', () => {
  it('should create an instance', () => {
    expect(new I18nInventoryLoader()).toBeTruthy();
  });
});
